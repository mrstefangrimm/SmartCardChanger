<Window x:Class="SmartCardStatusApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SmartCardStatusApp"
        mc:Ignorable="d"
        Title="Smart Card Status" Height="450" Width="800">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <Button Grid.Column="0"
            Content="Check"
            FontSize="24"
            AutomationProperties.AutomationId="CheckButtonAid"
            Click="Button_Click"/>
    <Label Grid.Column="1"
           Name="OutputLabel"
           FontSize="24"
           VerticalContentAlignment="Center"
           AutomationProperties.AutomationId="OutputLabelAid">
      <Label.Style>
        <Style TargetType="Label">
          <Setter Property="Foreground" Value="Red" />
          <Style.Triggers>
            <DataTrigger Binding="{Binding IsInserted}" Value="True">
              <Setter Property="Foreground" Value="Green" />
            </DataTrigger>
          </Style.Triggers>
        </Style>
      </Label.Style>
    </Label>
  </Grid>
</Window>
